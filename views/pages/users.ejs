<% include ../partials/header %>
<table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>email</th>
                <th>password</th>
                <th><a name="" id="" class="btn btn-success" href="/insert_user" role="button">Add Newuser</a></td></th>
            </tr>
        </thead>
        <tbody id="myTable">
        
    <% users.forEach(function(user){ %>
    
            <tr>
                <td scope="row"><%= user.id %></td>
                <td><%= user.email %></td>
                <td><%= user.password %></td>
                <td><a  class="btn btn-primary" href="/users/<%= user.id %>" role="button">Edit</a>
                <a name="" id="" class="btn btn-danger" href="/user_delete/<%=user.id %>" onclick="return confirm('Are you sure you want to delete?')" role="button">Delete</a></td>
            </tr>
            <tr>
                <td scope="row"></td>
                <td></td>
                <td></td>
            </tr>
            <% }); %>
        </tbody>
    
    </table>
<script>
$(document).ready(function(){
  $("#myInput").on("keyup", function() {
    var value = $(this).val().toLowerCase();
    $("#myTable tr").filter(function() {
      $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
    });
  });
});
</script>


<% include ../partials/footer %>