<% include ../partials/header %>
<div class="jumbotron">
<h1><center>PRODUCTS</center></h1><br>
<table class="table">
        <thead>
            <tr class="text-white bg-secondary">
                <th>ID</th>
                <th>title</th>
                <th>Price</th>
                
                <th><a name="" product_id="" class="btn btn-success" href="/insert" role="button">Add Newproduct</a></td></th>
            </tr>
        </thead>
        <tbody id="myTable">
        
    <% products.forEach(function(product){ %>
    
            <tr class="bg-light text-dark">
                <td scope="row"><%= product.product_id %></td>
                <td><%= product.title %></td>
                <td><%= product.price %></td>
                <td><a  class="btn btn-primary" href="/products/<%= product.product_id %>" role="button">Edit</a>
                <a name="" product_id="" class="btn btn-danger" href="/product_delete/<%=product.product_id %>" onclick="return confirm('Are you sure you want to delete?')" role="button">Delete</a></td>
            </tr>
           
            <tr>
                <td scope="row"></td>
                <td></td>
                <td></td>
            </tr>
            <% }); %>
        </tbody>
    
    </table>
<script>
$(document).ready(function(){
  $("#myInput").on("keyup", function() {
    var value = $(this).val().toLowerCase();
    $("#myTable tr").filter(function() {
      $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
    });
  });
});
</script>

</div>
<script>
        var selectedUser = 0;
        function deleteUser(id){
            console.log(id);
            selectedUser = id;
            $('#exampleModalCenter').modal('show')
        }
        function confirmDelete(){
            console.log('confirm:'+selectedUser);
            window.location.href = '/user_delete/'+selectedUser;
        }        
    </script>

<% include ../partials/footer %>